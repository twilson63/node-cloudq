// cloudq bulk update api
// cloudq run couchdb views
var flatiron = require('flatiron'),
  app = flatiron.app;

// cloudq web site
app.router.get('/bulk', function(name){
  this.res.end('You want to do a bulk update');
});