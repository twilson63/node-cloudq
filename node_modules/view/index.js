// cloudq run couchdb views
var flatiron = require('flatiron'),
  app = flatiron.app;

// cloudq web site
app.router.get('/view/:name', function(name){
  this.res.end('You want to get view ' + name);
});